declare type Options = {
    highlightTag: string;
    markdown: boolean;
    markdownStyle: string;
    fixationPoint: number;
};

declare const bionicReading: (text: string, maybeOptions?: Partial<Options>) => string;

export { bionicReading };
