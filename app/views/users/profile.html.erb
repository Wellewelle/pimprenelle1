<div class="container">

  <h1>Profile page</h1>

  <div>
    <%# Favoris %>
    <% @user.favorites.each do |favorite| %>
      <%= favorite.story.title %><br>
      <%= favorite.story.summary %>
      <%= button_to "delete to favorite", favorite_path(favorite, story_id: favorite.story.id), method: :delete, data: { confirm: 'Are you sure?' } %>
    <% end %>
  </div>

  <div>
    <%# Historique %>
  </div>

  <div>
    <%# Mes histoires (que j'ai créées)%>
  </div>

  <div>
    <%# Créer une histoire %>
  </div>
<%= link_to "Back to homepage", root_path %>

</div>
