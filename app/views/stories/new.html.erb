<div class="container">
  <h1>Ecrivez votre propre histoire</h1>

  <%= simple_form_for(@story) do |f| %>
    <%= f.input :title, label: "Titre" %>
    <%= f.input :summary, label: "Résumé" %>
    <%= f.input :tag, label: "Vos tags" %>
    <%= f.input :age, label: "Tranche d'âge" %>
    <%= f.input :content, label: "Racontez..." %>
    <%= f.button :submit, "Publier mon histoire", class: "submit-button"%>
  <% end %>
  <%= link_to "Back to profile", profile_path %>

</div>
